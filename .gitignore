.DS_Store

# Desktop 在 iCloud Drive 同步范围内。同步冲突时 iCloud 会生成 "i1 2.webp"
# 这种带空格加数字的重复文件，内容和正本一模一样，纯垃圾。
# 不挡掉的话，README 里那句 git add -A 会把它们一起提交上去。
#
# 副作用：照片别起 "chair 2.jpg" 这种「空格+数字」的名字，会被一起忽略。
# 要多张同名的用连字符：chair-2.jpg。
* [0-9].*
